<testcase>
	<brief>power off the AP</brief>
	<description>
		This testcase is used to power off APs
	</description>

<!-- 
	<stage>
		<step no="1">
			<desc>up AP1 through Remote Power Switch</desc>
			<log-file>rmpts.log</log-file>
			<script>
				${bin.dir}/rmtps.pl ${tb.rmps.ip} ${tb.rmps.user}:${tb.rmps.passwd} ${ap1.rmps_port}\off 1> ${log.dir}/${log.file} 2>/dev/null
			</script>
			<no-error-check />
		</step>
		<step no="2">
			<desc>up AP2 through Remote Power Switch</desc>
			<if>${job.numofap} > 1</if>
			<log-file>rmpts.log</log-file>
			<script>
				${bin.dir}/rmtps.pl ${tb.rmps.ip} ${tb.rmps.user}:${tb.rmps.passwd} ${ap2.rmps_port}\off 1>> ${log.dir}/${log.file} 2>/dev/null
			</script>
			<no-error-check />
		</step>
		<step no="3">
			<desc>up AP3 through Remote Power Switch</desc>
			<if>${job.numofap} > 2</if>
			<log-file>rmpts.log</log-file>
			<script>
				${bin.dir}/rmtps.pl ${tb.rmps.ip} ${tb.rmps.user}:${tb.rmps.passwd} ${ap3.rmps_port}\off 1>> ${log.dir}/${log.file} 2>/dev/null
			</script>
			<no-error-check />
		</step>
	</stage>
-->
</testcase>
